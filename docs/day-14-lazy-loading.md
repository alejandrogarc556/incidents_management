# Lazy Loading Benefits

Lazy loading allows Angular to load feature code only when it is needed.

Benefits:

- Smaller initial bundle.
- Faster application startup.
- Reduced memory usage.
- Better scalability.
- Independent feature routing.

Incident routes are loaded only when a user navigates to the incident section.